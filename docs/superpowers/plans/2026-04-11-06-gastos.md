# Renda Frag — Plano 6: Tela Gastos

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tela Gastos completa: lista de categorias expansíveis, fluxo de importação CSV com modal de tutorial + bottom sheet de confirmação, e deduplicação silenciosa por hash.

**Architecture:** `useImportarCsv` expõe dois métodos — `parseFile(file)` retorna o preview (síncrono, sem IO) e `importar.mutate(gastos)` salva no Supabase com upsert idempotente. A tela gerencia um estado de 3 fases (`idle → tutorial → preview`). Um `<input type="file">` oculto é acionado programaticamente. `useGastos` já existe do Plano 4.

**Tech Stack:** React, TanStack Query, Supabase, Tailwind, `csvParser.ts`, `categories.ts`, `hashGasto.ts` (todos do Plano 2)

**Pré-requisito:** Planos 1, 2, 3 e 4 concluídos.

---

## Mapa de arquivos

| Arquivo | O que faz |
|---|---|
| `src/hooks/useImportarCsv.ts` | `parseFile(file)` → preview; `importar.mutate(gastos)` → upsert no Supabase |
| `src/components/gastos/CsvTutorialModal.tsx` | Modal com passo a passo de exportação do Nubank |
| `src/components/gastos/CsvImportSheet.tsx` | Bottom sheet com resumo por categoria + botão confirmar |
| `src/components/gastos/CategoriaCard.tsx` | Card expansível: categoria + total + lista de itens |
| `src/pages/Gastos.tsx` | Página: monta tudo e gerencia estado do fluxo de import |

---

## Task 1: useImportarCsv

**Files:**
- Create: `src/hooks/useImportarCsv.ts`

- [ ] **Step 1: Implementar useImportarCsv**

```typescript
// src/hooks/useImportarCsv.ts
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { useAuth } from '../contexts/AuthContext'
import { supabase } from '../lib/supabase'
import { parseCsvNubank, GastoParseado } from '../lib/csvParser'
import { categorizar } from '../lib/categories'
import { hashGasto } from '../lib/hashGasto'
import { Categoria } from '../types'

export interface CategoriaSummary {
  categoria: Categoria
  total: number
  count: number
}

export interface PreviewImport {
  gastos: GastoParseado[]
  porCategoria: CategoriaSummary[]
  totalGeral: number
}

export async function parseFile(file: File): Promise<PreviewImport> {
  const content = await file.text()
  const { gastos } = parseCsvNubank(content)

  const acc: Record<string, CategoriaSummary> = {}
  for (const g of gastos) {
    const cat = categorizar(g.titulo) as Categoria
    if (!acc[cat]) acc[cat] = { categoria: cat, total: 0, count: 0 }
    acc[cat].total += g.valor
    acc[cat].count += 1
  }

  const porCategoria = Object.values(acc).sort((a, b) => b.total - a.total)
  const totalGeral = gastos.reduce((sum, g) => sum + g.valor, 0)

  return { gastos, porCategoria, totalGeral }
}

export function useImportarCsv() {
  const { user } = useAuth()
  const queryClient = useQueryClient()

  const importar = useMutation({
    mutationFn: async (gastos: GastoParseado[]) => {
      const rows = await Promise.all(
        gastos.map(async g => ({
          user_id: user!.id,
          valor: g.valor,
          titulo: g.titulo,
          categoria: categorizar(g.titulo),
          data: g.data,
          origem: 'csv',
          hash: await hashGasto(user!.id, g.data, g.titulo, g.valor),
        }))
      )

      // ignoreDuplicates: true — reimportações não geram erro, só ignoram
      const { error } = await supabase
        .from('gastos')
        .upsert(rows, { onConflict: 'user_id,hash', ignoreDuplicates: true })

      if (error) throw error
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['gastos'] })
    },
  })

  return { importar }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useImportarCsv.ts
git commit -m "feat: useImportarCsv — parse + upsert idempotente com deduplicação por hash"
```

---

## Task 2: CsvTutorialModal

**Files:**
- Create: `src/components/gastos/CsvTutorialModal.tsx`

- [ ] **Step 1: Implementar CsvTutorialModal**

```typescript
// src/components/gastos/CsvTutorialModal.tsx
interface Props {
  onContinuar: () => void
  onFechar: () => void
}

const PASSOS = [
  {
    tipo: 'Cartão de crédito',
    icone: '💳',
    instrucao: 'Abra o Nubank → toque em Cartão → selecione a fatura → "..." → Exportar fatura',
  },
  {
    tipo: 'Pix e débito',
    icone: '📲',
    instrucao: 'Abra o Nubank → toque em Conta → Extrato → escolha o período → Exportar',
  },
]

export default function CsvTutorialModal({ onContinuar, onFechar }: Props) {
  return (
    <div className="fixed inset-0 z-50 flex items-end justify-center">
      {/* Overlay */}
      <div
        className="absolute inset-0 bg-black/60"
        onClick={onFechar}
      />

      {/* Modal */}
      <div className="relative w-full max-w-md bg-slate-900 rounded-t-2xl p-6 pb-10">
        {/* Handle */}
        <div className="w-10 h-1 bg-slate-700 rounded-full mx-auto mb-5" />

        <h2 className="text-base font-bold mb-1">Como exportar do Nubank</h2>

        {/* O que é um CSV */}
        <div className="bg-slate-800 rounded-xl p-3 mb-4">
          <p className="text-xs text-slate-300 leading-relaxed">
            <span className="font-semibold text-white">O que é esse arquivo?</span>{' '}
            O Nubank permite exportar seus gastos como um arquivo de texto simples (CSV).
            É como uma planilha com data, nome do estabelecimento e valor de cada compra.
          </p>
        </div>

        <p className="text-xs text-slate-400 mb-4">
          Você pode importar os dois — cada um cobre um tipo de gasto.
        </p>

        <div className="space-y-4 mb-5">
          {PASSOS.map(passo => (
            <div key={passo.tipo} className="bg-slate-800 rounded-xl p-4">
              <div className="flex items-center gap-2 mb-2">
                <span className="text-lg">{passo.icone}</span>
                <span className="text-sm font-semibold">{passo.tipo}</span>
              </div>
              <p className="text-xs text-slate-400 leading-relaxed">{passo.instrucao}</p>
            </div>
          ))}
        </div>

        {/* Garantia de privacidade */}
        <div className="flex items-start gap-2 bg-slate-800/50 rounded-xl p-3 mb-5">
          <span className="text-sm mt-0.5">🔒</span>
          <p className="text-xs text-slate-400 leading-relaxed">
            O arquivo fica no seu celular. O app lê os valores e descarta o arquivo —
            nada mais é enviado. Só você tem acesso aos seus dados.
          </p>
        </div>

        <button
          onClick={onContinuar}
          className="w-full bg-indigo-600 hover:bg-indigo-500 rounded-xl py-3 text-sm font-semibold transition-colors"
        >
          Já tenho o arquivo →
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/gastos/CsvTutorialModal.tsx
git commit -m "feat: CsvTutorialModal — instruções de exportação Nubank (crédito e Pix)"
```

---

## Task 3: CsvImportSheet

**Files:**
- Create: `src/components/gastos/CsvImportSheet.tsx`

- [ ] **Step 1: Implementar CsvImportSheet**

```typescript
// src/components/gastos/CsvImportSheet.tsx
import { PreviewImport } from '../../hooks/useImportarCsv'
import { Categoria } from '../../types'
import { formatBRL } from '../../lib/fmt'

const ICONES: Record<Categoria, string> = {
  alimentacao: '🍔',
  transporte:  '🚗',
  moradia:     '🏠',
  saude:       '💊',
  lazer:       '🎮',
  assinaturas: '📱',
  educacao:    '📚',
  outros:      '📦',
}

interface Props {
  preview: PreviewImport
  onConfirmar: () => void
  onCancelar: () => void
  isPending: boolean
}

export default function CsvImportSheet({ preview, onConfirmar, onCancelar, isPending }: Props) {
  return (
    <div className="fixed inset-0 z-50 flex items-end justify-center">
      {/* Overlay */}
      <div className="absolute inset-0 bg-black/60" onClick={onCancelar} />

      {/* Sheet */}
      <div className="relative w-full max-w-md bg-slate-900 rounded-t-2xl p-6 pb-10">
        {/* Handle */}
        <div className="w-10 h-1 bg-slate-700 rounded-full mx-auto mb-5" />

        {/* Header */}
        <div className="text-center mb-5">
          <p className="text-base font-bold">
            {preview.gastos.length} gastos encontrados
          </p>
          <p className="text-sm text-slate-400 mt-0.5">
            Total: {formatBRL(preview.totalGeral)}
          </p>
        </div>

        {/* Resumo por categoria */}
        <div className="space-y-2 mb-6">
          {preview.porCategoria.map(({ categoria, total, count }) => (
            <div
              key={categoria}
              className="flex items-center justify-between bg-slate-800 rounded-xl px-4 py-3"
            >
              <div className="flex items-center gap-2">
                <span>{ICONES[categoria]}</span>
                <span className="text-sm capitalize">{categoria}</span>
                <span className="text-xs text-slate-500">{count} itens</span>
              </div>
              <span className="text-sm font-semibold text-red-400">
                {formatBRL(total)}
              </span>
            </div>
          ))}
        </div>

        {/* Botões */}
        <button
          onClick={onConfirmar}
          disabled={isPending}
          className="w-full bg-indigo-600 hover:bg-indigo-500 disabled:opacity-50 rounded-xl py-3 text-sm font-semibold transition-colors mb-3"
        >
          {isPending ? 'Importando...' : `Importar tudo ✓`}
        </button>
        <button
          onClick={onCancelar}
          className="w-full text-sm text-slate-500 hover:text-slate-400 py-2 transition-colors"
        >
          Cancelar
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/gastos/CsvImportSheet.tsx
git commit -m "feat: CsvImportSheet — bottom sheet com resumo de categorias antes de confirmar"
```

---

## Task 4: CategoriaCard

**Files:**
- Create: `src/components/gastos/CategoriaCard.tsx`

- [ ] **Step 1: Implementar CategoriaCard**

```typescript
// src/components/gastos/CategoriaCard.tsx
import { useState } from 'react'
import { Gasto, Categoria } from '../../types'
import { GastosPorCategoria } from '../../hooks/useSaldo'
import { formatBRL, formatDataCurta } from '../../lib/fmt'

const ICONES: Record<Categoria, string> = {
  alimentacao: '🍔',
  transporte:  '🚗',
  moradia:     '🏠',
  saude:       '💊',
  lazer:       '🎮',
  assinaturas: '📱',
  educacao:    '📚',
  outros:      '📦',
}

const CORES_BARRA: Record<Categoria, string> = {
  alimentacao: 'bg-amber-500',
  transporte:  'bg-blue-500',
  moradia:     'bg-violet-500',
  saude:       'bg-green-500',
  lazer:       'bg-pink-500',
  assinaturas: 'bg-cyan-500',
  educacao:    'bg-orange-500',
  outros:      'bg-slate-500',
}

interface Props {
  grupo: GastosPorCategoria
  totalGeral: number
}

export default function CategoriaCard({ grupo, totalGeral }: Props) {
  const [expandido, setExpandido] = useState(false)
  const cat = grupo.categoria as Categoria
  const porcent = totalGeral > 0 ? (grupo.total / totalGeral) * 100 : 0

  return (
    <div className="bg-slate-900 rounded-2xl overflow-hidden mb-2">
      {/* Header clicável */}
      <button
        className="w-full px-4 py-4 flex items-center gap-3 text-left"
        onClick={() => setExpandido(e => !e)}
      >
        <span className="text-xl">{ICONES[cat]}</span>
        <div className="flex-1 min-w-0">
          <div className="flex justify-between items-center mb-1.5">
            <span className="text-sm font-semibold capitalize">{cat}</span>
            <span className="text-sm font-bold text-red-400">{formatBRL(grupo.total)}</span>
          </div>
          {/* Barra proporcional */}
          <div className="h-1.5 bg-slate-800 rounded-full overflow-hidden">
            <div
              className={`h-full ${CORES_BARRA[cat]} rounded-full`}
              style={{ width: `${porcent}%` }}
            />
          </div>
          <p className="text-xs text-slate-500 mt-1">
            {grupo.itens.length} transações · toca para {expandido ? 'fechar' : 'ver'}
          </p>
        </div>
        <span className="text-slate-600 text-xs ml-1">{expandido ? '▲' : '▼'}</span>
      </button>

      {/* Lista expandida */}
      {expandido && (
        <div className="border-t border-slate-800">
          {grupo.itens.map(gasto => (
            <div
              key={gasto.id}
              className="flex items-center justify-between px-4 py-3 border-b border-slate-800/50 last:border-0"
            >
              <div>
                <p className="text-xs text-slate-300">{gasto.titulo}</p>
                <p className="text-xs text-slate-600 mt-0.5">{formatDataCurta(gasto.data)}</p>
              </div>
              <p className="text-xs font-semibold text-red-400">{formatBRL(gasto.valor)}</p>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/gastos/CategoriaCard.tsx
git commit -m "feat: CategoriaCard — card expansível com barra proporcional e lista de itens"
```

---

## Task 5: Montar Gastos.tsx

**Files:**
- Modify: `src/pages/Gastos.tsx`

- [ ] **Step 1: Substituir Gastos.tsx**

```typescript
// src/pages/Gastos.tsx
import { useRef, useState } from 'react'
import { useGastos } from '../hooks/useGastos'
import { useImportarCsv, parseFile, PreviewImport } from '../hooks/useImportarCsv'
import { GastosPorCategoria } from '../hooks/useSaldo'
import { Categoria } from '../types'
import { formatBRL, formatDataCurta } from '../lib/fmt'
import CsvTutorialModal from '../components/gastos/CsvTutorialModal'
import CsvImportSheet from '../components/gastos/CsvImportSheet'
import CategoriaCard from '../components/gastos/CategoriaCard'

type EstadoImport = 'idle' | 'tutorial' | 'preview'

function agruparPorCategoria(gastos: ReturnType<typeof useGastos>['data']): GastosPorCategoria[] {
  if (!gastos) return []
  const acc: Record<string, GastosPorCategoria> = {}
  for (const g of gastos) {
    const cat = g.categoria as Categoria
    if (!acc[cat]) acc[cat] = { categoria: cat, total: 0, itens: [] }
    acc[cat].total += g.valor
    acc[cat].itens.push(g)
  }
  return Object.values(acc).sort((a, b) => b.total - a.total)
}

export default function Gastos() {
  const { data: gastos = [], isLoading } = useGastos()
  const { importar } = useImportarCsv()
  const fileInputRef = useRef<HTMLInputElement>(null)
  const [estadoImport, setEstadoImport] = useState<EstadoImport>('idle')
  const [preview, setPreview] = useState<PreviewImport | null>(null)

  const grupos = agruparPorCategoria(gastos)
  const totalGastos = gastos.reduce((sum, g) => sum + g.valor, 0)

  // Data do último import (mais recente created_at)
  const ultimoImport = gastos.length > 0
    ? gastos.reduce((max, g) => (g.created_at > max ? g.created_at : max), gastos[0].created_at)
    : null

  async function handleFileChange(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0]
    if (!file) return
    e.target.value = '' // permite reimportar o mesmo arquivo
    const resultado = await parseFile(file)
    setPreview(resultado)
    setEstadoImport('preview')
  }

  function handleConfirmarImport() {
    if (!preview) return
    importar.mutate(preview.gastos, {
      onSuccess: () => {
        setEstadoImport('idle')
        setPreview(null)
      },
    })
  }

  return (
    <div className="p-4">
      {/* Header */}
      <div className="flex items-start justify-between mb-4">
        <div>
          <h1 className="text-lg font-bold">Gastos</h1>
          {isLoading ? (
            <p className="text-xs text-slate-500 mt-0.5">Carregando...</p>
          ) : (
            <>
              <p className="text-2xl font-bold text-red-400 mt-1">{formatBRL(totalGastos)}</p>
              {ultimoImport && (
                <p className="text-xs text-slate-600 mt-0.5">
                  atualizado em {formatDataCurta(ultimoImport.split('T')[0])}
                </p>
              )}
            </>
          )}
        </div>
        <button
          onClick={() => setEstadoImport('tutorial')}
          className="flex items-center gap-1.5 bg-slate-800 hover:bg-slate-700 rounded-xl px-3 py-2 text-xs font-medium text-slate-300 transition-colors mt-1"
        >
          <span>📄</span>
          <span>CSV</span>
        </button>
      </div>

      {/* Input de arquivo oculto */}
      <input
        ref={fileInputRef}
        type="file"
        accept=".csv"
        onChange={handleFileChange}
        className="hidden"
      />

      {/* Estado vazio */}
      {!isLoading && grupos.length === 0 && (
        <div className="bg-slate-900 rounded-2xl p-8 text-center">
          <p className="text-3xl mb-3">📄</p>
          <p className="text-sm font-semibold mb-1">Importe seu extrato Nubank</p>
          <p className="text-xs text-slate-400 mb-4">
            Crédito ou Pix/débito — categorização automática
          </p>
          <button
            onClick={() => setEstadoImport('tutorial')}
            className="bg-indigo-600 hover:bg-indigo-500 rounded-xl px-5 py-2.5 text-sm font-semibold transition-colors"
          >
            Escolher arquivo CSV
          </button>
        </div>
      )}

      {/* Lista de categorias */}
      {grupos.map(grupo => (
        <CategoriaCard
          key={grupo.categoria}
          grupo={grupo}
          totalGeral={totalGastos}
        />
      ))}

      {/* Modal de tutorial */}
      {estadoImport === 'tutorial' && (
        <CsvTutorialModal
          onContinuar={() => {
            setEstadoImport('idle')
            fileInputRef.current?.click()
          }}
          onFechar={() => setEstadoImport('idle')}
        />
      )}

      {/* Bottom sheet de confirmação */}
      {estadoImport === 'preview' && preview && (
        <CsvImportSheet
          preview={preview}
          onConfirmar={handleConfirmarImport}
          onCancelar={() => { setEstadoImport('idle'); setPreview(null) }}
          isPending={importar.isPending}
        />
      )}
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/Gastos.tsx
git commit -m "feat: tela Gastos — categorias expansíveis + fluxo CSV completo"
```

---

## Task 6: Teste manual do fluxo completo de importação

- [ ] **Step 1: Testar com CSV de crédito**

```bash
npm run dev
```

1. Abrir aba Gastos → estado vazio aparece
2. Tocar "📄 CSV" → modal de tutorial abre com instruções
3. Tocar "Já tenho o arquivo →" → seletor de arquivo nativo abre
4. Selecionar um CSV de crédito Nubank (ou criar um arquivo de teste):

```csv
date,title,amount
2024-04-05,MERCADO PAO DE ACUCAR,87.50
2024-04-08,Uber *Viagem,23.00
2024-04-10,NETFLIX.COM,45.90
2024-04-12,IFOOD,38.00
```

5. Bottom sheet abre com resumo por categoria + total
6. Tocar "Importar tudo ✓" → gastos aparecem na lista agrupados
7. Tocar em uma categoria → expande com os itens individuais

- [ ] **Step 2: Testar reimportação (deduplicação)**

Importar o mesmo arquivo novamente. Nenhum gasto novo deve ser adicionado — a contagem permanece igual.

- [ ] **Step 3: Verificar na Home**

Voltar para a aba Home. Verificar:
- "↓ Saiu" no card de resumo mostra os gastos importados
- "Onde foi o dinheiro?" mostra as barras de categoria
- Ritmo do mês atualizado com os novos gastos

- [ ] **Step 4: Commit final**

```bash
git add -A
git commit -m "feat: fluxo de importação CSV testado e funcionando"
```

---

## Verificação final do Plano 6

Ao final deste plano, o projeto deve:

- [ ] Botão "📄 CSV" abre modal de tutorial com instruções para crédito e Pix
- [ ] Após selecionar arquivo, bottom sheet mostra resumo por categoria
- [ ] Confirmar importação → gastos aparecem agrupados por categoria
- [ ] Reimportar o mesmo arquivo → sem duplicatas (contagem não muda)
- [ ] Cada categoria expande para mostrar os itens individuais
- [ ] Home reflete os gastos importados imediatamente
- [ ] `npx tsc --noEmit` → zero erros
