# Renda Frag — Plano 5: Tela Entradas

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tela Entradas funcional: formulário inline sempre visível no topo (valor, fonte, data) e lista do mês abaixo com total em destaque.

**Architecture:** `useEntradas` já existe do Plano 4. Este plano adiciona `useAdicionarEntrada` (mutation) e os dois componentes de UI. A página `Entradas.tsx` monta tudo. Ao salvar, TanStack Query invalida o cache de entradas e a Home atualiza automaticamente.

**Tech Stack:** React, TanStack Query, Supabase, Tailwind

**Pré-requisito:** Planos 1, 2, 3 e 4 concluídos (`useEntradas`, `fmt.ts` e `datas.ts` já existem).

---

## Mapa de arquivos

| Arquivo | O que faz |
|---|---|
| `src/hooks/useAdicionarEntrada.ts` | Mutation: insere entrada no Supabase e invalida cache |
| `src/components/entradas/EntradaForm.tsx` | Formulário inline: valor + fonte + data + botão Salvar |
| `src/components/entradas/EntradaLista.tsx` | Lista do mês com total em destaque e estado vazio |
| `src/pages/Entradas.tsx` | Página: monta formulário + lista |

---

## Task 1: useAdicionarEntrada

**Files:**
- Create: `src/hooks/useAdicionarEntrada.ts`

- [ ] **Step 1: Implementar useAdicionarEntrada**

```typescript
// src/hooks/useAdicionarEntrada.ts
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { useAuth } from '../contexts/AuthContext'
import { supabase } from '../lib/supabase'

interface NovaEntrada {
  valor: number
  fonte: string
  data: string
}

export function useAdicionarEntrada() {
  const { user } = useAuth()
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async (entrada: NovaEntrada) => {
      const { error } = await supabase
        .from('entradas')
        .insert({ ...entrada, user_id: user!.id })
      if (error) throw error
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['entradas'] })
    },
  })
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useAdicionarEntrada.ts
git commit -m "feat: useAdicionarEntrada — mutation que insere entrada e invalida cache"
```

---

## Task 2: EntradaForm

**Files:**
- Create: `src/components/entradas/EntradaForm.tsx`

- [ ] **Step 1: Implementar EntradaForm**

```typescript
// src/components/entradas/EntradaForm.tsx
import { useState, FormEvent } from 'react'
import { useAdicionarEntrada } from '../../hooks/useAdicionarEntrada'

function hojeISO(): string {
  return new Date().toISOString().split('T')[0]
}

export default function EntradaForm() {
  const [valor, setValor] = useState('')
  const [fonte, setFonte] = useState('')
  const [data, setData] = useState(hojeISO())
  const { mutate: adicionar, isPending } = useAdicionarEntrada()

  function handleSubmit(e: FormEvent) {
    e.preventDefault()
    const valorNum = parseFloat(valor.replace(',', '.'))
    if (isNaN(valorNum) || valorNum <= 0 || !fonte.trim()) return

    adicionar(
      { valor: valorNum, fonte: fonte.trim(), data },
      {
        onSuccess: () => {
          setValor('')
          setFonte('')
          setData(hojeISO())
        },
      }
    )
  }

  return (
    <form
      onSubmit={handleSubmit}
      className="bg-slate-900 rounded-2xl p-4 mb-4 border border-indigo-500/30"
    >
      <p className="text-xs text-indigo-400 font-semibold uppercase tracking-wide mb-3">
        + Nova entrada
      </p>

      {/* Campo valor */}
      <div className="flex items-center bg-slate-950 rounded-xl px-4 py-3 mb-3 gap-2 focus-within:ring-1 focus-within:ring-emerald-500 transition-all">
        <span className="text-slate-500 text-sm font-medium">R$</span>
        <input
          type="number"
          value={valor}
          onChange={e => setValor(e.target.value)}
          placeholder="0,00"
          min="0.01"
          step="0.01"
          required
          className="flex-1 bg-transparent text-2xl font-bold text-emerald-400 focus:outline-none placeholder:text-slate-700"
        />
      </div>

      {/* Fonte e data lado a lado */}
      <div className="flex gap-2 mb-3">
        <input
          type="text"
          value={fonte}
          onChange={e => setFonte(e.target.value)}
          placeholder="Fonte (ex: salário, freela)"
          required
          className="flex-1 bg-slate-950 rounded-xl px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-indigo-500 placeholder:text-slate-700 transition-all"
        />
        <input
          type="date"
          value={data}
          onChange={e => setData(e.target.value)}
          required
          className="bg-slate-950 rounded-xl px-3 py-2 text-sm text-slate-300 focus:outline-none focus:ring-1 focus:ring-indigo-500 transition-all"
        />
      </div>

      <button
        type="submit"
        disabled={isPending}
        className="w-full bg-emerald-700 hover:bg-emerald-600 disabled:opacity-50 rounded-xl py-2.5 text-sm font-semibold transition-colors"
      >
        {isPending ? 'Salvando...' : 'Salvar entrada'}
      </button>
    </form>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/entradas/EntradaForm.tsx
git commit -m "feat: EntradaForm — formulário inline com valor em destaque"
```

---

## Task 3: EntradaLista

**Files:**
- Create: `src/components/entradas/EntradaLista.tsx`

- [ ] **Step 1: Implementar EntradaLista**

```typescript
// src/components/entradas/EntradaLista.tsx
import { Entrada } from '../../types'
import { formatBRL, formatData } from '../../lib/fmt'

interface Props {
  entradas: Entrada[]
  total: number
}

export default function EntradaLista({ entradas, total }: Props) {
  return (
    <div>
      {/* Total do mês */}
      <div className="flex items-center justify-between mb-3">
        <p className="text-sm text-slate-400">Total recebido no mês</p>
        <p className="text-lg font-bold text-emerald-400">{formatBRL(total)}</p>
      </div>

      {/* Estado vazio */}
      {entradas.length === 0 && (
        <div className="bg-slate-900 rounded-2xl p-6 text-center">
          <p className="text-2xl mb-2">💰</p>
          <p className="text-sm text-slate-400">Nenhuma entrada registrada este mês.</p>
          <p className="text-xs text-slate-600 mt-1">Use o formulário acima para adicionar.</p>
        </div>
      )}

      {/* Lista */}
      <div className="space-y-2">
        {entradas.map(entrada => (
          <div
            key={entrada.id}
            className="bg-slate-900 rounded-xl px-4 py-3 flex items-center justify-between"
          >
            <div>
              <p className="text-sm font-medium text-slate-200">{entrada.fonte}</p>
              <p className="text-xs text-slate-500 mt-0.5">{formatData(entrada.data)}</p>
            </div>
            <p className="text-sm font-bold text-emerald-400">{formatBRL(entrada.valor)}</p>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/entradas/EntradaLista.tsx
git commit -m "feat: EntradaLista — lista do mês com total e estado vazio"
```

---

## Task 4: Montar Entradas.tsx

**Files:**
- Modify: `src/pages/Entradas.tsx`

- [ ] **Step 1: Substituir Entradas.tsx**

```typescript
// src/pages/Entradas.tsx
import { useEntradas } from '../hooks/useEntradas'
import EntradaForm from '../components/entradas/EntradaForm'
import EntradaLista from '../components/entradas/EntradaLista'

export default function Entradas() {
  const { data: entradas = [], isLoading } = useEntradas()
  const total = entradas.reduce((sum, e) => sum + e.valor, 0)

  return (
    <div className="p-4">
      <h1 className="text-lg font-bold mb-4">Entradas</h1>

      <EntradaForm />

      {isLoading ? (
        <p className="text-sm text-slate-500 text-center py-8">Carregando...</p>
      ) : (
        <EntradaLista entradas={entradas} total={total} />
      )}
    </div>
  )
}
```

- [ ] **Step 2: Testar fluxo completo no browser**

```bash
npm run dev
```

Verificar:
1. Abrir aba "Entradas" → formulário visível no topo, lista vazia com estado vazio abaixo
2. Preencher valor (ex: 2500), fonte (ex: "Salário"), data → clicar "Salvar entrada"
3. Formulário reseta → entrada aparece na lista imediatamente (TanStack Query invalida cache)
4. Total atualiza corretamente
5. Voltar para Home → "↑ Entrou" no card de resumo mostra o valor recém-adicionado

- [ ] **Step 3: Commit**

```bash
git add src/pages/Entradas.tsx
git commit -m "feat: tela Entradas — formulário inline + lista do mês completos"
```

---

## Verificação final do Plano 5

Ao final deste plano, o projeto deve:

- [ ] Formulário aceita valor, fonte e data e salva no Supabase
- [ ] Após salvar, formulário reseta e entrada aparece na lista sem recarregar a página
- [ ] Total do mês atualiza imediatamente após cada adição
- [ ] Home → card "↑ Entrou" reflete as entradas adicionadas
- [ ] Estado vazio exibido quando não há entradas no mês
- [ ] `npx tsc --noEmit` → zero erros
