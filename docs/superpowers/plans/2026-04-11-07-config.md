# Renda Frag — Plano 7: Tela Config

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tela Config completa: três campos editáveis inline com borda colorida (dinheiro guardado, meta de poupança, contas fixas), email do usuário e botão de logout.

**Architecture:** `CampoEditavel` é um componente genérico que alterna entre modo leitura e modo edição inline. `useAtualizarPerfil` é uma mutation que atualiza campos do perfil no Supabase. `usePerfil` (já existe do Plano 3) fornece os valores atuais. Ao salvar qualquer campo, o cache do perfil é invalidado e a Home recalcula o saldo automaticamente.

**Tech Stack:** React, TanStack Query, Supabase, Tailwind

**Pré-requisito:** Planos 1, 2, 3 e 4 concluídos (`usePerfil` e `AuthContext` já existem).

---

## Mapa de arquivos

| Arquivo | O que faz |
|---|---|
| `src/hooks/useAtualizarPerfil.ts` | Mutation: atualiza campos do perfil no Supabase |
| `src/components/config/CampoEditavel.tsx` | Campo com modo leitura/edição inline e borda colorida |
| `src/pages/Config.tsx` | Página: monta os 3 campos + seção de conta |

---

## Task 1: useAtualizarPerfil

**Files:**
- Create: `src/hooks/useAtualizarPerfil.ts`

- [ ] **Step 1: Implementar useAtualizarPerfil**

```typescript
// src/hooks/useAtualizarPerfil.ts
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { useAuth } from '../contexts/AuthContext'
import { supabase } from '../lib/supabase'
import { Perfil } from '../types'

type CamposPerfil = Partial<Pick<
  Perfil,
  'dinheiro_guardado' | 'meta_poupanca_mensal' | 'gastos_fixos_mensais' |
  'renda_mensal_estimada' | 'nome'
>>

export function useAtualizarPerfil() {
  const { user } = useAuth()
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async (campos: CamposPerfil) => {
      const { error } = await supabase
        .from('perfis')
        .update(campos)
        .eq('id', user!.id)
      if (error) throw error
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['perfil'] })
    },
  })
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useAtualizarPerfil.ts
git commit -m "feat: useAtualizarPerfil — mutation que atualiza campos do perfil e invalida cache"
```

---

## Task 2: CampoEditavel

**Files:**
- Create: `src/components/config/CampoEditavel.tsx`

O componente tem dois modos:
- **Leitura:** mostra label, valor formatado e ícone de lápis
- **Edição:** substitui o valor por um input numérico; confirma com Enter ou ao perder o foco

- [ ] **Step 1: Implementar CampoEditavel**

```typescript
// src/components/config/CampoEditavel.tsx
import { useState, useRef, useEffect } from 'react'
import { formatBRL } from '../../lib/fmt'

interface Props {
  label: string
  subtexto: string
  valor: number
  corBorda: string   // classe Tailwind, ex: 'border-emerald-500'
  aoSalvar: (novoValor: number) => void
  salvando?: boolean
}

export default function CampoEditavel({
  label,
  subtexto,
  valor,
  corBorda,
  aoSalvar,
  salvando = false,
}: Props) {
  const [editando, setEditando] = useState(false)
  const [input, setInput] = useState('')
  const inputRef = useRef<HTMLInputElement>(null)

  useEffect(() => {
    if (editando) {
      setInput(valor.toFixed(2).replace('.', ','))
      inputRef.current?.focus()
      inputRef.current?.select()
    }
  }, [editando, valor])

  function confirmar() {
    const novoValor = parseFloat(input.replace(',', '.'))
    if (!isNaN(novoValor) && novoValor >= 0) {
      aoSalvar(novoValor)
    }
    setEditando(false)
  }

  function handleKeyDown(e: React.KeyboardEvent) {
    if (e.key === 'Enter') confirmar()
    if (e.key === 'Escape') setEditando(false)
  }

  return (
    <div className={`bg-slate-900 rounded-2xl p-4 border-l-4 ${corBorda}`}>
      <p className="text-xs text-slate-400 font-medium uppercase tracking-wide mb-1">
        {label}
      </p>

      {editando ? (
        <div className="flex items-center gap-2 mb-1">
          <span className="text-slate-500 text-sm">R$</span>
          <input
            ref={inputRef}
            type="number"
            value={input}
            onChange={e => setInput(e.target.value)}
            onBlur={confirmar}
            onKeyDown={handleKeyDown}
            min="0"
            step="0.01"
            className="flex-1 bg-transparent text-2xl font-bold text-white focus:outline-none"
          />
        </div>
      ) : (
        <button
          onClick={() => setEditando(true)}
          disabled={salvando}
          className="flex items-center gap-2 mb-1 group"
        >
          <span className="text-2xl font-bold text-white">
            {salvando ? '...' : formatBRL(valor)}
          </span>
          <span className="text-slate-600 group-hover:text-slate-400 transition-colors text-sm">
            ✏️
          </span>
        </button>
      )}

      <p className="text-xs text-slate-600 leading-relaxed">{subtexto}</p>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/config/CampoEditavel.tsx
git commit -m "feat: CampoEditavel — edição inline com borda colorida e confirmação por Enter/blur"
```

---

## Task 3: Montar Config.tsx

**Files:**
- Modify: `src/pages/Config.tsx`

- [ ] **Step 1: Substituir Config.tsx**

```typescript
// src/pages/Config.tsx
import { usePerfil } from '../hooks/usePerfil'
import { useAtualizarPerfil } from '../hooks/useAtualizarPerfil'
import { useAuth } from '../contexts/AuthContext'
import CampoEditavel from '../components/config/CampoEditavel'

export default function Config() {
  const { user, signOut } = useAuth()
  const { data: perfil, isLoading } = usePerfil()
  const { mutate: atualizar, isPending } = useAtualizarPerfil()

  if (isLoading) {
    return (
      <div className="min-h-screen flex items-center justify-center text-slate-500 text-sm">
        Carregando...
      </div>
    )
  }

  return (
    <div className="p-4">
      <h1 className="text-lg font-bold mb-6">Config</h1>

      <div className="space-y-3 mb-8">
        <CampoEditavel
          label="Dinheiro guardado"
          subtexto="Toca para editar. Afeta seu saldo disponível."
          valor={perfil?.dinheiro_guardado ?? 0}
          corBorda="border-emerald-500"
          salvando={isPending}
          aoSalvar={v => atualizar({ dinheiro_guardado: v })}
        />

        <CampoEditavel
          label="Meta de poupança / mês"
          subtexto="Reservado antes de calcular o disponível."
          valor={perfil?.meta_poupanca_mensal ?? 0}
          corBorda="border-violet-500"
          salvando={isPending}
          aoSalvar={v => atualizar({ meta_poupanca_mensal: v })}
        />

        <CampoEditavel
          label="Contas fixas / mês"
          subtexto="Aluguel, internet, assinaturas — o que chega todo mês."
          valor={perfil?.gastos_fixos_mensais ?? 0}
          corBorda="border-blue-500"
          salvando={isPending}
          aoSalvar={v => atualizar({ gastos_fixos_mensais: v })}
        />
      </div>

      {/* Seção conta */}
      <div className="bg-slate-900 rounded-2xl overflow-hidden">
        <div className="px-4 py-3 border-b border-slate-800 flex items-center justify-between">
          <div>
            <p className="text-xs text-slate-500 mb-0.5">Conta</p>
            <p className="text-sm text-slate-200">{user?.email}</p>
          </div>
        </div>
        <button
          onClick={signOut}
          className="w-full px-4 py-3 text-left text-sm text-red-400 hover:bg-slate-800 transition-colors"
        >
          Sair da conta
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/Config.tsx
git commit -m "feat: tela Config — campos editáveis inline + logout"
```

---

## Task 4: Teste manual e verificação final

- [ ] **Step 1: Testar edição de campos**

```bash
npm run dev
```

1. Abrir aba Config
2. Tocar no valor de "Dinheiro guardado" → campo vira input, valor atual selecionado
3. Digitar novo valor → pressionar Enter ou clicar fora
4. Valor atualiza imediatamente na tela
5. Ir para Home → saldo disponível reflete o novo valor

- [ ] **Step 2: Testar os 3 campos**

Editar cada um dos três campos:
- Dinheiro guardado: afeta saldo no Hero da Home
- Meta de poupança: afeta saldo no Hero da Home  
- Contas fixas: afeta indicador de ritmo na Home (barra e ritmoOk)

- [ ] **Step 3: Testar Escape e blur**

- Editar campo → pressionar Escape → valor original restaurado, sem salvar
- Editar campo → clicar fora → valor novo salvo

- [ ] **Step 4: Testar logout**

Tocar "Sair da conta" → sessão encerrada → redireciona para `/auth`.

- [ ] **Step 5: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Esperado: zero erros.

- [ ] **Step 6: Rodar todos os testes**

```bash
npx vitest run
```

Esperado: 36 testes passando (da lib/), nenhum falhando.

- [ ] **Step 7: Commit final**

```bash
git add -A
git commit -m "feat: MVP Fase 0 completo — todas as telas implementadas"
```

---

## Verificação final do Plano 7 (e do MVP)

Ao final deste plano, o MVP completo deve:

- [ ] **Auth:** criar conta, fazer login, logout funcionando
- [ ] **Onboarding:** 5 perguntas em chat, salva no perfil, não repete após completo
- [ ] **Home:** saldo correto, estado verde/amarelo/vermelho, ritmo do mês, resumo, categorias
- [ ] **Entradas:** adicionar entrada atualiza Home imediatamente
- [ ] **Gastos:** importar CSV Nubank (crédito ou Pix), sem duplicatas em reimportação
- [ ] **Config:** editar dinheiro guardado, meta e contas fixas reflete na Home
- [ ] **36 testes** da lib/ passando com `npx vitest run`
- [ ] **Zero erros** TypeScript com `npx tsc --noEmit`
- [ ] **Build** sem erros com `npm run build`
